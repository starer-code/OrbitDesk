一：最大小化窗口出现的点击不响应bug
问题原因：
从debug输出中发现，窗口最大化后状态变成了 WindowFullScreen 而不是 WindowMaximized ，导致原来的条件判断失败，无法正确恢复到正常状态。

解决方案：
修改了条件判断，让它同时检查 WindowMaximized 和 WindowFullScreen 状态，这样无论窗口处于哪种最大化状态，都能正确地恢复到正常状态。